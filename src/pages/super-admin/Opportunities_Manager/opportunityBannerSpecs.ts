/** Dimensions affichage public (page détail opportunité, ratio 21:9). */
export const OPPORTUNITY_BANNER_SPECS = {
  recommendedWidth: 1920,
  recommendedHeight: 822,
  minWidth: 1200,
  minHeight: 514,
  aspectRatioLabel: "21:9",
  formatLabel: "1920 × 822 px",
  minFormatLabel: "1200 × 514 px",
} as const;

export { OPPORTUNITY_BANNER_DISPLAY_CLASS } from "./components/OpportunityPublicBannerFrame";
